// src/components/Elements/Image/Image.jsx
import React from "react";

const ImageIndex = ({ src, alt, className }) => {
  return <img src={src} alt={alt} className={className} />;
};

export default ImageIndex;
